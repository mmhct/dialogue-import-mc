# DialogueForge 1.0 验证记录

验证日期：2026-09-19。以下区分实际运行、自动检查和未验证项。

## 软件和结构生成

- Windows 发布物：`PE32+ executable (GUI), x86-64`，Go 交叉编译，所有界面资源内置。
- `go test -race ./...` 通过；`go vet ./...` 通过。
- 线路检查覆盖 6 个宽度与 10 个间隔组合，包括不超出宽度、连续连接、无交叉短接、供电距离、命令方块数量和总中继器延时。
- TXT 检查覆盖 LF、CRLF、CR、UTF-8 BOM、UTF-16、空行、同角色连续对白。
- 检查 JSON 转义、中文、表情符号、确定性结构输出、无效参数和本机 API。
- 最终 Windows EXE 在 macOS 的 Wine 11.17 中实际运行；成功读取中文路径 TXT 并导出 `.schem`。输出与本机编译版本逐字节一致。
- 最终 EXE 启动的本机 HTTP 服务可用；结构导出接口输出与命令行版本逐字节一致。
- 浏览器实际操作验证：TXT 文件选择导入、示例载入、逐句修改、最大宽度修改、线路预览、设置文件载入、无效间隔拦截。导出按钮收到成功响应。测试用内置浏览器未返回下载事件，未把其文件落盘列为通过；结构文件落盘已由 EXE 命令行验证。

## Minecraft 实际导入与播放

环境：隔离的本机 Paper **1.21.1 build 133**，WorldEdit **7.3.8**，Java 21。测试客户端通过 Minecraft 协议连接，真实执行 `//schem load`、`//paste` 并按下起点按钮。以下全部检查中文、引号、反斜杠及对白顺序；每个结构重复播放两次。

| 场景 | 对白数 | 实际宽 × 长 | 中继器数 | 结果 |
|---|---:|---:|---:|---|
| 单句 | 1 | 3 × 2 | 0 | 两轮通过 |
| 0.1 秒连续对白，多次掉头 | 18 | 8 × 18 | 17 | 两轮通过 |
| 每句不同间隔，多次掉头 | 10 | 8 × 21 | 28 | 两轮通过 |
| 16 格宽、1 秒间隔 | 12 | 16 × 10 | 33 | 两轮通过 |
| 64 格宽上限、2 秒间隔 | 6 | 33 × 2 | 25 | 两轮通过 |
| 8 格宽长线路、跨区块 | 24 | 8 × 74 | 115 | 两轮通过 |

以上共 12 轮、142 条输出，实际对白逐条比对通过。读取每个命令方块的 `LastExecution`，其相对游戏刻均严格等于预期中继器延时。测试把游戏刻速率设为 100 以加快执行，按游戏刻检查时间；完成后恢复为 20。

另外，在正常 **20 TPS** 下重复验证 6 句、2 秒间隔结构两轮。相对执行刻为 `0, 40, 80, 120, 160, 200`，两轮均通过。

表情符号单独通过 RCON 读取已导入的命令方块 `Command` 验证，确认 `😀` 保存正确。测试客户端的聊天 NBT 解码器会损坏 Java UTF-16 代理对，因此未将它的表情符号聊天显示当作 Minecraft 渲染结果。

## 具体修正

- 中继器方块状态的 `facing` 指向输入方向；预览箭头表示输出方向。结构序列化已按实际游戏语义转换，并由整条线路播放确认。
- WorldEdit 7.3.8 的字符串读取使用 Java `DataInput.readUTF`，结构采用 modified UTF-8。不能把普通四字节 UTF-8 直接写入表情符号的位置。
- 修正无效逐句间隔在修改其他参数后可能重新启用导出的界面问题。

## 验证边界

- **没有在原生 Windows 10/11 电脑上测试**。Wine 下的 EXE 运行验证不等于原生 Windows、系统默认浏览器关联和浏览器下载目录验证。
- 实测服务器环境为上述 Paper + WorldEdit。没有把 Fabric、NeoForge、FAWE、其他 Minecraft 版本或第三方红石优化模组列为实测通过。
- 超大结构、服务器掉 TPS、区块卸载和播放中重复按按钮不属于精确秒数保证范围。
- 表情符号字符保存已验证；具体字形能否显示取决于 Minecraft 客户端字体或资源包。

## 复验入口

常规检查：`go test -race ./...`、`go vet ./...`。

游戏验证脚本：`scripts/game_validation.cjs`。它只连接 `127.0.0.1:25579` 的专用测试服，使用本机 `25589` 的测试 RCON，不连接任何公共或用户现有服务器。需自行准备相同版本测试服、WorldEdit、Java 和开发依赖 `mineflayer`，并启用命令方块。

脚本期望本机开发可执行文件 `.validation/dialogueforge` 和测试服务端目录 `.validation/server/`。可用 `VALIDATION_CASE=wide VALIDATION_TICK_RATE=20` 单独复验正常速度的场景。

实际检查结果在开发工作区 `.validation/game-results*.json`。这些是生成结果，没有作为运行软件的依赖打包。
