# 对白工坊 DialogueForge v1.1.0

为 Minecraft Java 1.21.1 生成命令方块与红石线路。本版支持调整掉头间距、混合对白和指令、多文件合并与分块，并跳过 TXT 注释行。

## 下载和使用

- **DialogueForge-1.1.0-Windows-x64.zip**：推荐下载，内含 Windows EXE、中文说明、旧版与新版示例、验证记录。
- **DialogueForge.exe**：单独的 Windows x86-64 程序，已内置界面及运行所需资源。
- **DialogueForge-1.1.0-source.zip**：源代码归档。
- **SHA256SUMS.txt**：发布附件的 SHA-256 校验值。

解压后双击 EXE，软件会打开默认浏览器中的本机界面。选择 TXT、调整文件顺序并导入，编辑剧情后导出。单个结构是 `.schem`，多个结构打包成 ZIP。解压并通过 WorldEdit 导入，在金色方块上按按钮播放。

## 本次更新

- **掉头间距可调**：两条主线之间的空格数可从 1 起设置，默认 3，不允许 0。
- **原始指令**：TXT 行首为 `/` 时直接写入命令方块，去掉首个 `/`，不包裹 `tellraw`。界面可编辑内容、选择类型、调整顺序，并插入传送、给予、标题、效果及自定义指令。
- **合并与分块**：多份 TXT 可排序后合成一个结构；支持按条数、按来源文件或手动分界拆成多个结构。每块可单独预览和导出，整批 ZIP 带清单与可编辑项目。
- **注释行**：TXT 行首为 `#` 时跳过整行，不生成方块、不占用延时、不计入分块条数。
- 项目最多 10000 条，每块最多 2000 条，一次最多 256 块；旧项目仍可载入，未保存间距的项目按 3 格处理。

每块都有独立启动按钮，块末条间隔不生效，块之间不会自动连接。指令模板需要自行补全参数；软件不替代 Minecraft 完整语法检查。`/`、`#` 必须是 TXT 行的第一个字符。

## 已验证与限制

300 组线路参数检查、Go race 测试和 vet 通过。在 Paper 1.21.1 build 133 + WorldEdit 7.3.8 中完成 14 个结构、28 轮播放检查，362 次命令方块执行的顺序与游戏刻间隔全部通过，覆盖 1/2/3/7/40 格间距、指令实际效果及 ZIP 分块独立播放。正常 20 TPS 下另测两轮混合指令，通过。

Windows EXE 已在 Wine 11.17 中运行，多文件分块 ZIP 与本机版本逐字节一致。Safari 实际下载的多结构 ZIP 已核验。**尚未在原生 Windows 电脑上实测。**

导出的文件为 WorldEdit `.schem`，不是原版结构方块 `.nbt`。游戏中需启用命令方块并保持整条线路所在区块加载；掉 TPS 会增加实际等待时间。当前版本不包含剧情分支、暂停或防重复启动功能。

[使用说明](https://github.com/mmhct/dialogue-import-mc/blob/main/README.md) · [完整验证记录](https://github.com/mmhct/dialogue-import-mc/blob/main/VALIDATION.md)
